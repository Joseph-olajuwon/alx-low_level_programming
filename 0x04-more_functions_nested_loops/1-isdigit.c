#include "main.h>"
/**
 * _indigit - function checks for digit (0 through 9)
 * @c: inter to be checked
 * Return: 1 if c is a digit, 0 if not
 */
int _indigit(int c)
{
	return (c >= '0' && c <= '9');
}
